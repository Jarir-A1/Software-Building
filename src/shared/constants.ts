// Shared constants used across the main, preload, and renderer processes.

// The canonical application name. Must remain exactly 'WordSetu'.
export const APP_NAME = 'WordSetu';

// Semantic version of the application. Kept in sync with package.json.
export const APP_VERSION = '0.2.0';

// The default local dev server URL used by the main process during development.
export const DEV_SERVER_ENV = 'WORDSETU_DEV_SERVER';
